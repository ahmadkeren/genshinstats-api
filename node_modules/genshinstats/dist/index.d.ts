import { UserInfo } from './structure';
interface Options {
    cookie: string;
}
export declare class GenshinStats {
    private server;
    private headers;
    private request;
    private DS;
    private cookie;
    constructor(options: Options);
    getUserInfo(uid: number): Promise<UserInfo>;
}
export {};
